var common_logininit={};
common_logininit.name="ログイン画面初期化";//
common_logininit.paramsFormat={ 

};

common_logininit.fire=function(params){   //

	var ret = new Result();

	session.invalidate();

	return ret;

};

